#include "main.h"

/**
 * _abs - A function that computes the absolute value of an integer.
 * @d: compute the absolute value of an integer.
 *
 * Return: The absolute value.
 */

int _abs(int d)
{
	if (d >= 0)
{
		return (d);
}
	else
{
		return (d * -1);
}
}
